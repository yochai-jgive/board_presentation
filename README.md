# Board Presentation

A React-based interactive slide deck (built with Recharts).

## Run locally
npm install
npm run dev

## Deploy
Push this repo to GitHub, then import it in Vercel or Netlify (auto-detects Vite).
Every push to the main branch redeploys automatically to the same live URL.
